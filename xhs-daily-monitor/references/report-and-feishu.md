# 日报与飞书追加

## 日报结构

1. `YYYY-MM-DD｜监控主题`。
2. 采集口径：关键词、时间窗口、回补情况、互动门槛、实际数量。
3. 结果表格。
4. 今日洞察。
5. 数据限制与排除说明。
6. 运行信息：适配器、运行时间、去重、排除和失败数量。

结果列为：发布时间/时间层、标题与链接、作者、赞/藏/评、摘要、类型/备注、`note_id`。

洞察只基于当日样本，区分多篇共性与个案，并披露样本量和口径限制。

## 写入步骤

1. 使用飞书文档能力，并完整读取其使用说明。
2. 写入前读取目标文档，验证 URL、权限、当天标题及已有 `note_id`。
3. 仅允许 `append`，禁止 `overwrite`。一次运行整段追加一次，不逐篇写。
4. 当天标题不存在时追加完整日报。
5. 当天标题已存在时执行配置：
   - `skip`：不写。
   - `append_new_only`：剔除已有 `note_id`，只追加“补充 HH:mm”；无新笔记则不写。
   - `append_timed_supplement`：追加带时间的补充章节，仍先去重。
6. 动态内容中的 `&`、`<`、`>` 必须 XML 转义；追加块以 `<h2>` 开始，不写 `<title>`。
7. 检查返回的 `result`、`updated_blocks_count` 和 `warnings`。只有 `success` 或经核实的 `partial_success` 可报告写入结果。
8. 写入后再次读取新增标题或文末片段。无法验证时明确写“已提交写入但未完成复核”。

CLI 语义示例：

```text
lark-cli docs +update --api-version v2 --as user \
  --doc "客户确认的文档 URL" \
  --command append \
  --content "生成并正确转义的 XML"
```

## 完成回报

```text
执行日期与时区：
实际使用的浏览器适配器：
关键词数量：
首选时间窗口有效数：
回补有效数：
最终唯一笔记数：
重复 / 排除 / 失败数量：
互动口径及部分可见数量：
飞书写入状态：成功 / 部分成功 / 未写入
飞书文档链接：
限制与下次建议：
```
